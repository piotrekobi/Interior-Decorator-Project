import { Component } from "react";
import styles from  "./SpawnZone.module.css"

export default class SpawnZone extends Component{
    render() {
        return(
            <div className={styles.spawnZone}></div>
        )
    }
}